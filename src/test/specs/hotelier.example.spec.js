(function() {
  'use strict';

  let expect = chai.expect;
  
  describe('hotelier module', function() {
    it('one plus one equals two', function(){
      expect(1+1).to.equal(2);
    });
  });
}());
